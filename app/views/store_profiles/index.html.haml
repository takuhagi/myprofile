= render 'layouts/header'
.list-page
  .jumbotron.jumbotron-fluid.p-4
    .container
      %h1.display-4.mb-4
        店舗一覧
      %p
        This is an introduction of recommended stores.

      = render 'layouts/search_list_page'

  %section.container
    .row

      -# ここから佐久間さん指定の6枚
      -if @priority_store_profile1 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile1.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile1.image.present?
                  = image_tag(@priority_store_profile1.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile1 != nil
                    = @priority_store_profile1.name
                - if @priority_store_profile1 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile1.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile1 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile1.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile1 != nil
                    = @priority_store_profile1.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -# - else
      -#   .col-lg-4
      -#     .card.mb-3
      -#       %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
      -#       .card-body{style: "height: 276px; position: relative;"}
      -#         %h5.card-title
      -#           .small.text-muted
      -#         .small.text-muted{style: "margin-bottom: 12px"}
      -#         %p.card-text
      -#         %div{style: "position: absolute; left: 20px; bottom: 20px"}
      -#         %div{style: "position: absolute; right: 32px; bottom: 28px"}
      -#           %p.text-muted
      -#             ★PR



      -if @priority_store_profile2 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          =link_to store_profile_path(@priority_store_profile2.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile2.image.present?
                  = image_tag(@priority_store_profile2.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile2 != nil
                    = @priority_store_profile2.name
                - if @priority_store_profile2 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile2.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile2 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile2.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile2 != nil
                    = @priority_store_profile2.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -# - else
      -#   .col-lg-4
      -#     .card.mb-3
      -#       %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
      -#       .card-body{style: "height: 276px; position: relative;"}
      -#         %h5.card-title
      -#           .small.text-muted
      -#         .small.text-muted{style: "margin-bottom: 12px"}
      -#         %p.card-text
      -#         %div{style: "position: absolute; left: 20px; bottom: 20px"}
      -#         %div{style: "position: absolute; right: 32px; bottom: 28px"}
      -#           %p.text-muted
      -#             ★PR

      -if @priority_store_profile3 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile3.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile3.image.present?
                  = image_tag(@priority_store_profile3.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile3 != nil
                    = @priority_store_profile3.name
                - if @priority_store_profile3 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile3.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile3 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile3.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile3 != nil
                    = @priority_store_profile3.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -# - else
      -#   .col-lg-4
      -#     .card.mb-3
      -#       %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
      -#       .card-body{style: "height: 276px; position: relative;"}
      -#         %h5.card-title
      -#           .small.text-muted
      -#         .small.text-muted{style: "margin-bottom: 12px"}
      -#         %p.card-text
      -#         %div{style: "position: absolute; left: 20px; bottom: 20px"}
      -#         %div{style: "position: absolute; right: 32px; bottom: 28px"}
      -#           %p.text-muted
      -#             ★PR

      -if @priority_store_profile4 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile4.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile4.image.present?
                  = image_tag(@priority_store_profile4.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile4 != nil
                    = @priority_store_profile4.name
                - if @priority_store_profile4 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile4.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile4 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile4.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile4 != nil
                    = @priority_store_profile4.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -# - else
      -#   .col-lg-4
      -#     .card.mb-3
      -#       %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
      -#       .card-body{style: "height: 276px; position: relative;"}
      -#         %h5.card-title
      -#           .small.text-muted
      -#         .small.text-muted{style: "margin-bottom: 12px"}
      -#         %p.card-text
      -#         %div{style: "position: absolute; left: 20px; bottom: 20px"}
      -#         %div{style: "position: absolute; right: 32px; bottom: 28px"}
      -#           %p.text-muted
      -#             ★PR

      -if @priority_store_profile5 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile5.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile5.image.present?
                  = image_tag(@priority_store_profile5.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile5 != nil
                    = @priority_store_profile5.name
                - if @priority_store_profile5 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile5.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile5 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile5.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile5 != nil
                    = @priority_store_profile5.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -# - else
      -#   .col-lg-4
      -#     .card.mb-3
      -#       %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
      -#       .card-body{style: "height: 276px; position: relative;"}
      -#         %h5.card-title
      -#           .small.text-muted
      -#         .small.text-muted{style: "margin-bottom: 12px"}
      -#         %p.card-text
      -#         %div{style: "position: absolute; left: 20px; bottom: 20px"}
      -#         %div{style: "position: absolute; right: 32px; bottom: 28px"}
      -#           %p.text-muted
      -#             ★PR

      -if @priority_store_profile6 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile6.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile6.image.present?
                  = image_tag(@priority_store_profile6.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile6 != nil
                    = @priority_store_profile6.name
                - if @priority_store_profile6 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile6.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile6 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile6.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile6 != nil
                    = @priority_store_profile6.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -if @priority_store_profile7 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile7.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile7.image.present?
                  = image_tag(@priority_store_profile7.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile7 != nil
                    = @priority_store_profile7.name
                - if @priority_store_profile7 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile7.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile7 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile7.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile7 != nil
                    = @priority_store_profile7.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -if @priority_store_profile8 != nil
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(@priority_store_profile8.id) do
            .card.commonButton
              .card-image
                -if @priority_store_profile8.image.present?
                  = image_tag(@priority_store_profile8.image.url, class: "card-img-top")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body{style: "position: relative;"}
                %h5.card-title
                  - if @priority_store_profile8 != nil
                    = @priority_store_profile8.name
                - if @priority_store_profile8 != nil
                  .small.text-muted
                    = "投稿者：#{@priority_store_profile8.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  - if @priority_store_profile8 != nil
                    = "最終更新：#{time_ago_in_words(@priority_store_profile8.updated_at)}前"
                %p.card-text
                  - if @priority_store_profile8 != nil
                    = @priority_store_profile8.explanation.truncate(30)
                %div{style: "position: absolute; right: 16px; bottom: 12px"}
                  %p.text-muted
                    ★PR
      -# - else
      -#   .col-lg-4
      -#     .card.mb-3
      -#       %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
      -#       .card-body{style: "height: 276px; position: relative;"}
      -#         %h5.card-title
      -#           .small.text-muted
      -#         .small.text-muted{style: "margin-bottom: 12px"}
      -#         %p.card-text
      -#         %div{style: "position: absolute; left: 20px; bottom: 20px"}
      -#         %div{style: "position: absolute; right: 32px; bottom: 28px"}
      -#           %p.text-muted
      -#             ★PR

              -# ここまで佐久間さん指定の6枚

      - @store_profiles.each do |store_profile|
        .col-lg-3.col-md-4.col-sm-6.col-xs-2.mb-4
          = link_to store_profile_path(store_profile.id) do
            .card.commonButton
              .card-image
                - if store_profile.image.present?
                  = image_tag(store_profile.image.url, alt: "メイン写真", class: "card-img-top no_image")
                - else
                  %img.card-img-top{alt: "...", src: "/assets/no_image.png"}/
              .card-body
                %h5.card-title
                  = store_profile.name
                .small.text-muted
                  = "投稿者：#{store_profile.user.nickname}"
                .small.text-muted{style: "margin-bottom: 12px"}
                  = "最終更新：#{time_ago_in_words(store_profile.updated_at)}前"
                %p.card-text
                  = store_profile.explanation.truncate(30)

    %section
      %p
        = page_entries_info @store_profiles
      .paginate.mb-4
        = paginate @store_profiles
