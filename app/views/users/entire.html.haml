-# 佐久間さんのみしか入れないようにする
-# =binding.pry
-# - @store_profile_1.each do |store_profile_1|
-#   - if store_profile_1.priority_number == 1
-#     = store_profile_1.name
-#   - else
-#     -# idは、佐久間さんのid。今回は仮で1にしている
-#     -# 選択ボックスにしたい
-#     = form_with url: user_entire_update_path(id: 1, priority_number: 1), method: :patch do |f|
-#       aaa
-#       = f.submit
-#       %br

-# - for i in 1..6
-#   - @store_profiles.each do |store_profile|
-#     店舗優先順位#{i}位
-#     - if store_profile.priority_number == i
-#       %br
-#       = store_profile.name
-#       %br
-#       = form_with url: user_entire_delete_priority_number_path(id: 1, priority_number: 0), method: :patch do |f|
-#         = f.submit "取り消し"
-#     - else
-#       -# idは、佐久間さんのid。今回は仮で1にしている
-#       -# 選択ボックスにしたい
-#       = form_with url: user_entire_update_path(id: 1, priority_number: i), method: :patch do |f|
-#         -# = f.select @store_profiles, include_blank: "洗濯してください"
-#         = f.collection_select :priority_number, @store_profiles, :priority_number, :name, inculde_blank: "選択してください"
-#         = f.submit "選択"
-#         %br
-#         %br





-# - if @store_profile_1 != []
-#   - @store_profile_1.each do |store_profile|
-#     店舗優先順位1位
-#     - if store_profile.priority_number == 1
-#       %br
-#       = store_profile.name
-#       %br
-#       = form_with url: user_entire_delete_priority_number_path(id: store_profile.id, priority_number: 0), method: :patch do |f|
-#         = f.submit "取り消し"
-#     - else
-#       -# -# idは、佐久間さんのid。今回は仮で1にしている
-#       -# -# 選択ボックスにしたい
-#       -# = form_with url: user_entire_update_path(id: store_profile.id, priority_number: 1), method: :patch do |f|
-#       -#   = f.select @store_profiles, include_blank: "選択してください"
-#       -#   -# = f.collection_select :priority_number, @store_profiles, :priority_number, :name, inculde_blank: "選択してください"
-#       -#   = f.submit "選択"
-#       -#   %br
-#       -#   %br
-# - else
-#   = form_with url: user_entire_update_path, method: :patch do |f|
-#     = f.select :priority_number, @store_profiles_select_array, include_blank: "選択してください"
    
-#     -# = f.collection_select :id, @store_profiles, :id, :name, inculde_blank: "選択してください"
-#     -# = f.collection_select :priority_number, @store_profiles, :priority_number, :name, inculde_blank: "選択してください"
-#     = f.submit "選択"
-#     %br
-#     %br

-# - if @store_profile_1 != []
-#   - @store_profile_1.each do |store_profile|
-#     店舗優先順位1位
-#     - if store_profile.priority_number == 1
-#       %br
-#       = store_profile.name
-#       %br
-#       = form_with url: user_entire_delete_priority_number_path(id: store_profile.id, priority_number: 0), method: :patch do |f|
-#         = f.submit "取り消し"
-#     - else
-#       -# -# idは、佐久間さんのid。今回は仮で1にしている
-#       -# -# 選択ボックスにしたい
-#       -# = form_with url: user_entire_update_path(id: store_profile.id, priority_number: 1), method: :patch do |f|
-#       -#   = f.select @store_profiles, include_blank: "選択してください"
-#       -#   -# = f.collection_select :priority_number, @store_profiles, :priority_number, :name, inculde_blank: "選択してください"
-#       -#   = f.submit "選択"
-#       -#   %br
-#       -#   %br
-# - else
店舗の順位を決める
%br
= form_with url: priority_store_profiles_path, method: :post do |f|
  = f.select :store_profile_id, @store_profiles_select_array, include_blank: "店舗を選択してください"
  = f.select :priority_number, @priority_number_array, include_blank: "優先順位を選択してください"
  -# = f.collection_select :id, @store_profiles, :id, :name, inculde_blank: "選択してください"
  -# = f.collection_select :priority_number, @store_profiles, :priority_number, :name, inculde_blank: "選択してください"
  = f.submit "選択"
  %br
  %br

現在の順位
%br
- PriorityStoreProfile.all.each do |store_profile|
  - StoreProfile.where(id: store_profile.store_profile_id).each do |s_profile|
    = s_profile.name
    = store_profile.priority_number
    = form_with url: priority_store_profile_path(id: store_profile.id), method: :delete do |f|
      = f.submit "削除"
    %br
    
    -# = form_with url: 

-# 店舗
-# 優先順位1位
-# 表示、削除
-# or
-# 選択

-# 優先順位2位
-# 優先順位3位
-# 優先順位4位
-# 優先順位5位
-# 優先順位6位


-# 商品
-# 優先順位1位
-# 優先順位2位
-# 優先順位3位
-# 優先順位4位
-# 優先順位5位
-# 優先順位6位

-# サービス
-# 優先順位1位
-# 優先順位2位
-# 優先順位3位
-# 優先順位4位
-# 優先順位5位
-# 優先順位6位

-# イベント
-# 優先順位1位
-# 優先順位2位
-# 優先順位3位
-# 優先順位4位
-# 優先順位5位
-# 優先順位6位
